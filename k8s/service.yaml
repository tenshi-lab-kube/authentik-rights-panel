apiVersion: v1
kind: Service
metadata:
  name: authentik-rights-panel
  namespace: authentik-rights-panel
  labels:
    app.kubernetes.io/name: authentik-rights-panel
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: authentik-rights-panel
  ports:
    - name: http
      port: 80
      targetPort: http
